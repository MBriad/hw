---
name: 架构审查报告更新
overview: 基于代码库当前状态的全面架构审查，更新 ARCHITECTURE-REVIEW.md
todos:
  - content: 整理已修复项与新增项
    status: completed
    priority: high
  - content: 撰写更新后的 ARCHITECTURE-REVIEW.md
    status: in_progress
    priority: high
---

# Plan: 更新 ARCHITECTURE-REVIEW.md

## 对比旧审查的变更摘要

### 已修复（旧报告中的问题）

| 旧编号 | 旧问题 | 状态 |
|--------|--------|------|
| R-1 | HeatmapPage 直接调用 @kit.ArkData preferences | ✅ 已修复 — HeatmapPage 无 @kit.* import |
| R-2 | ThemeProvider 直接调用 @kit.ArkData | ✅ 已修复 — ThemeProvider.ets 已删除 |
| R-3 | 主题持久化 3 处重复 | ✅ 已修复 — SkinProvider port 扩展了 loadPersisted()/persist()，SkinProviderImpl 统一实现 |
| R-4 | DebugRuntime 用 object literal 创建 EasterEgg | ✅ 已修复 — egg() 改用 new EasterEgg() |
| M-1 | HeatmapContent.ets 孤岛 | ✅ 已修复 — 文件已删除 |
| M-2 | Index.ets Hello World 残留 | ✅ 已修复 — 文件已删除 |
| M-3 | FrequencyPatternEngine 在 domain 层 | ✅ 已修复 — 已移至 adapters/ |
| M-6 | StreakCalculator port 无实现无调用 | ✅ 已修复 — port 已删除 |
| S-2 | TypographyStyles.ets 未使用 | ✅ 已修复 — 文件已删除 |
| S-3 | GlassTheme.ets 未使用 | ✅ 已修复 — 文件已删除 |
| S-6 | DebugRuntime.failure() 参数类型 Object | ✅ 已修复 — 改为 Error |
| M-8 | ParticleOverlay 硬编码颜色 | ✅ 已修复 — 改用 tokens.brandPrimary 等动态颜色 |
| M-9 | CELL_SIZE 重复硬编码 | ✅ 已修复 — 移入 Spacing.calendarCell |
| S-5 | EasterEgg/CheckIn 是 interface | ✅ 已修复 — 均改为 class |

### 仍然存在的问题

| 旧编号 | 旧问题 | 当前状态 |
|--------|--------|---------|
| R-5 | @Observed + @StorageLink 不匹配 | ⚠️ 仍存在 |
| M-5 | LoadHeatmap level 只设 0/1 | ⚠️ 仍存在 |
| M-7 | SoundPlayerImpl 缺 eva_alert 映射 | ⚠️ 仍存在 |
| M-10 | 两套彩蛋检测逻辑并存 | ⚠️ 部分：DetectEasterEgg usecase 已不在 VM 中，但 IDetectEasterEgg port 仍存在 |
| S-4 | VMFixture 依赖 SkinProviderImpl | ⚠️ 需确认 |
| M-4 | LearnedPatternPolicy 直接实例化 | ⚠️ 仍存在 |

### 新增问题

| 编号 | 新问题 | 严重度 |
|------|--------|--------|
| N-1 | HeatmapVM 直接 import AnalyzePendingObservations 具体类（无 port） | 🔴 |
| N-2 | AnalyzePendingObservations 引用 PatternLearningApi.loadPendingBatch/acknowledgeProcessed 但旧 mock 可能未更新 | 🟡 |
| N-3 | ThemePalette 仍使用 object literal + `as DesignTokens`（4 处） | 🟡 |
| N-4 | PreferencesLearnedPatternRepo 返回 JSON.parse as LearnedPattern[] 未用构造函数重建 | 🟡 |
| N-5 | domain/SkinTheme.ets 隐式依赖 ResourceColor 全局类型 | 🟢 |
| N-6 | HeatmapPage.aboutToAppear() 的 initialize().then() 无 .catch() | 🔴 |
| N-7 | HeatmapVM 9/10 async 方法无 try/catch | 🔴 |
| N-8 | soundPlayer.play() fire-and-forget（无 await/.catch） | 🟡 |
| N-9 | HeatmapGrid ForEach cells 缺 key generator | 🔴 |
| N-10 | Streak.ets 只剩 HeatmapCell，StreakInfo/StreakLevel 已消失但 DayCell 注释仍引用 level 0-4 | 🟡 |
| N-11 | 63 处硬编码 fontSize、15+ 处硬编码 height/width、21 处硬编码 border width | 🟡 |
| N-12 | @Prop 数组深拷贝性能问题（cells 经 2 层传递） | 🟢 |

## 设计

更新后的报告结构：

1. 审查范围与方法
2. 已修复项回顾
3. 🔴 严重问题（4 项）
4. 🟡 中等问题（8 项）
5. 🟢 改善建议（4 项）
6. 架构合规矩阵
7. 修复优先级路线图